{% extends "base.html.twig" %}

{% block body %}
    <div>
        {% for quiz in quizzes %}
            {{ quiz.quizNum }}. {{ quiz.question }}<br />
            {{ results[quiz.quizNum] }}<br />
            正解:{{ quiz.correctAns }}
            あなたの解答:{{ user_choices[quiz.quizNum] }}<br /><br />
        {% endfor %}

        <br />
        <a href="{{ path('four_course_list') }}"><<大問一覧へ</a>
    </div>
{% endblock %}